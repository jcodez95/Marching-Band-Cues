{
  "name": "cuesheet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A mobile-first PWA for playing back audio recordings and dropping timestamped notes on them. Built with plain HTML/CSS/JS — no framework, no build step. This package.json exists only to run the dev-time db.js tests in Node; the app itself ships with zero dependencies.",
  "scripts": {
    "test": "node scripts/test-db.mjs && node scripts/test-fingerprint.mjs && node scripts/test-scrub-rate.mjs && node scripts/test-time-format.mjs && node scripts/test-onset-detect.mjs && node scripts/test-metronome.mjs && node scripts/test-cuesheet-io.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
